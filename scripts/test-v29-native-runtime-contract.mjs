import fs from "node:fs"; const r=f=>fs.readFileSync(f,"utf8"), ok=(v,m)=>{if(!v)throw new Error(m)};
const n=r("lib/native-runtime.ts"),m=r("app/mais/page.tsx"),b=r("hooks/useBiometric.ts"),no=r("lib/notifications.ts"),d=r("lib/dose-notifications.ts"),w=r(".github/workflows/build-android.yml");
ok(n.includes("Capacitor.getPlatform()"),"runtime sem getPlatform"); ok(n.includes('typeof window === "undefined"'),"runtime sem proteção SSR"); ok(m.includes("setNativeRuntime(getVaultRuntime())"),"Mais não resolve runtime após mount"); ok(m.includes('const isNativeApp = nativeRuntime !== "web"'),"Mais sem estado nativo"); ok(!m.includes("Capacitor.isNativePlatform()"),"Mais ainda decide ambiente diretamente"); ok(b.includes("isVaultNative()"),"biometria fora do runtime único"); ok(no.includes("isVaultNative()")&&no.includes("isVaultAndroid()"),"notificações fora do runtime único"); ok(d.includes("isVaultNative()"),"doses fora do runtime único"); ok(w.includes("validate-android-bridge-v29.mjs"),"workflow sem validação V29"); ok(w.indexOf("validate-android-bridge-v29.mjs")>w.lastIndexOf("npx cap sync android"),"validação V29 antes do último cap sync"); console.log("V29 CONTRATOS OK");
